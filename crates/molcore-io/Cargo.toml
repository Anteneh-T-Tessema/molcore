[package]
name    = "molcore-io"
version = "0.1.0"
edition = "2021"

[dependencies]
petgraph = { workspace = true }
serde    = { workspace = true }
# arrow2 and parquet2 added when columnar I/O milestone lands
